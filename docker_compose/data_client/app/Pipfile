# Указываем источники пакетов
[[source]]
url = "https://pypi.python.org/simple"
# Указываем подтверждение через ssl и название пакета
verify_ssl = true
name = "pypi"

# Пакеты, предназнаяенные для требований разработки
# Например: библиотеки для прокерки кода, библиотеки для юнит текстов и т.д.
# Все это не нужно на машине пользователя
[dev-packages]

# Пакеты, необходимы для работы приложения с указанием версии
[packages]
numpy = "==1.19.0"
psycopg2-binary = "==2.8.6"
pymongo = "==3.11.0"
redis = "==3.5.5"
msgpack-python = "==1.0.0"
jupyter = "==1.0.0"
jupyter-client = "==6.1.7"
sqlalchemy = "==1.3.20"
pandas = "==1.1.3"
scipy = "==1.5.3"
scikit-learn = "==0.23.2"
matplotlib = "==3.3.2"
plotly = "==4.11.0"
pillow = "==8.0.0"
nltk = "==3.5"

# Зависимости
[requires]
# python_version = "3.6"
python_version = "3.9"
